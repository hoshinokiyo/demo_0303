<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.TodoMapper">

    <resultMap id="todoResultMap" type="Todo">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="completed" column="completed"/>
        <result property="currentUser" column="owner_name"/>
    </resultMap>

    <select id="findAll" resultMap="todoResultMap">
        SELECT id, title, completed, owner_name
        FROM todos
        ORDER BY id DESC
    </select>

    <select id="findAllByCurrentUser" parameterType="string" resultMap="todoResultMap">
        SELECT id, title, completed, owner_name
        FROM todos
        WHERE owner_name = #{currentUser}
        ORDER BY id
    </select>

    <select id="findById" parameterType="long" resultMap="todoResultMap">
        SELECT id, title, completed, owner_name
        FROM todos
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="Todo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO todos (title, completed, owner_name)
        VALUES (#{title}, #{completed}, #{currentUser})
    </insert>

    <update id="update" parameterType="Todo">
        UPDATE todos
        SET title = #{title},
            completed = #{completed},
            owner_name = #{currentUser}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM todos
        WHERE id = #{id}
    </delete>

</mapper>
